const blueprint = require('./blueprint');
const myBlueprint = require('./myBlueprint');
const component = require('./component');
const product = require('./product');

module.exports = router = {
  blueprint,
  myBlueprint,
  component,
  product
}